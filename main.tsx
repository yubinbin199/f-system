import React from 'react'
import ReactDOM from 'react-dom/client'
import BankMappingSystem from './BankMappingSystem'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BankMappingSystem />
  </React.StrictMode>,
)

